/**
 * Shared DOM rendering helpers: result cards (single) and result rows (batch).
 * Browser-only. (M5)
 *
 * @param {object} report - VerificationReport (see docs/IMPLEMENTATION_PLAN.md §6)
 * @returns {HTMLElement}
 */
export function renderReport(report) {
  throw new Error('renderReport: not implemented (M5)');
}
