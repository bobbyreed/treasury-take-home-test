/**
 * Batch flow: multi-file upload (optionally paired to an expected-values CSV),
 * bounded-concurrency queue (~6), progress, results table with a "needs review"
 * filter, and CSV export. Browser-only. (M6)
 *
 * @param {HTMLElement} root - container element to render into
 */
export function initBatch(root) {
  throw new Error('initBatch: not implemented (M6)');
}
