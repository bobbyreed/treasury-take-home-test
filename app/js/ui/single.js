/**
 * Single-label flow: upload image -> confirm/enter expected values + beverage
 * type -> Verify -> result card. Accessible (large targets, contrast, keyboard,
 * words+icon not color alone). Browser-only. (M5)
 *
 * @param {HTMLElement} root - container element to render into
 */
export function initSingle(root) {
  throw new Error('initSingle: not implemented (M5)');
}
